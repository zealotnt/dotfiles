---------------------------------------- REFERENCES ----------------------------------------
-- ref: https://www.postgresql.org/docs/current/app-psql.html
-- ref: https://www.citusdata.com/blog/2017/07/16/customizing-my-postgres-shell-using-psqlrc/
-- ref: https://thoughtbot.com/blog/improving-the-command-line-postgres-experience#there39s-more-to-life-than-prompts

---------------------------------------- OUR-CONFIG ----------------------------------------
-- By default, NULL displays as an empty space. Is it actually an empty
-- string, or is it null? This makes that distinction visible.
\pset null '(null)'

-- ref https://www.postgresql.org/docs/9.6/app-psql.html#APP-PSQL-PROMPTING
-- BOLD-BLUE-%M[%/]
-- explaination for color code
-- %[%033[1m%] -> BOLD
-- %[%033[34m%] -> BLUE
-- %[%033[37m%] -> WHITE
-- %M -> The full host name
-- %[%033[31m%] -> RED
-- [%/] --> [The name of current database]
-- %[%033[32m%] -> GREEN
-- [%n] -> [session user name]
-- %[%033[0m%] -> NORMAL
--  \n# -> The rest, SQL will be print in next prompt
\set PROMPT1 '%[%033[1m%]%[%033[37m%]HOST:%[%033[34m%]%M% %[%033[37m%]DB:%[%033[31m%][%/] %[%033[37m%]USR:%[%033[32m%][%n]%[%033[0m%] \n# '
-- SELECT * FROM<enter>. %R shows what type of input it expects.
\set PROMPT2 '... > '

-- Show how long each query takes to execute
\timing

-- Use table format (with headers across the top) by default, but switch to
-- expanded table format when there's a lot of data, which makes it much
-- easier to read.
\x auto

-- Verbose error reports.
\set VERBOSITY verbose

-- Use a separate history file per-database.
\set HISTFILE ~/.psql_history- :DBNAME

-- If a command is run more than once in a row, only store it once in the
-- history.
\set HISTCONTROL ignoredups

-- Autocomplete keywords (like SELECT) in upper-case, even if you started
-- typing them in lower case.
\set COMP_KEYWORD_CASE upper

-- Echo the actual queries generated by \d and other backslash commands.
-- For study purpose
\set ECHO_HIDDEN on

